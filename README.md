# finesseXcitadel

A systematic, long-only equity strategy for the Finesse × Citadel Round 2 portfolio
construction challenge. At most 10 stocks from the Nifty 100 and Nifty Midcap 100 indices,
backtested 1 Jan 2021 – 31 Dec 2025 on ₹1 crore with 0.1% transaction costs, and
stress-tested on 1 Jan – 30 Jun 2026.

Full narrative, no code required: **[`WALKTHROUGH.md`](WALKTHROUGH.md)**.
Every design choice and its reasoning: **[`DECISIONS.md`](DECISIONS.md)**.
Every configuration ever evaluated, including the failures: **`CLAUDE.md` §11**.

## The strategy, in one paragraph

Rank every eligible stock by its **12-1 momentum** — the return from 252 trading days ago
to 21 trading days ago, measured through yesterday's close. Buy the **top 10 in equal
weight**, execute at the next morning's open, and **repeat monthly**, resetting each
position back to 1/10. That is the entire rule. There is no optimiser, no covariance
estimate, no risk overlay and **no fitted parameter** — which is the point, and the defence.

## Results

Main window, 2021–25, after costs. Generated by `scripts/06_report.py`; see
[`output/report/numbers.md`](output/report/numbers.md) for the full required-metric set.

| | Total Net PNL | Return | CAGR | Sharpe | Max DD |
|---|---|---|---|---|---|
| **Submitted: monthly rebalance, equal weight** | **₹10,76,49,806** | **+1,076.5%** | 63.8% | 2.42 | −32.4% |
| Baseline: same rule, quarterly | ₹8,76,46,846 | +876.5% | 57.8% | 2.21 | −32.5% |
| Benchmark: equal-weight universe (costed) | ₹2,80,54,515 | +280.6% | — | — | — |
| Benchmark: Nifty 100 index | ₹89,41,008 | +89.4% | — | — | — |

**The strategy beats all 10,000 random 10-stock portfolios** drawn from the same eligible
set on the same dates through the same engine, and sits at the **96th percentile** of that
same band once return is measured per unit of risk. The configuration was chosen on 2021–25
alone; H1 2026 is used only as a one-way rejection filter, which it passes at +7.60%
against the Nifty 100's −6.65%.

## Is there anything better? 62 configurations say no

Every axis was swept, each **pre-registered with its predictions before it ran**, each
scored against a noise band drawn beforehand. Nothing beat the simple rule:

| Axis | Cells | Result |
|---|---|---|
| Rebalance cadence × weighting rule | 8 | **Monthly + reset wins**, +₹2.00 Cr over quarterly |
| Momentum lookback × skip | 6 | **The frozen 252/21 is the best cell of its own surface** |
| Adding Nifty Smallcap 100 | 2 | **Loses** — −₹4.39 Cr at the submitted cadence |
| A three-feature composite signal | 4 | **All lose**, −5.2σ to −7.5σ |
| That composite's feature weights | 42 | **0 of 42 beat the baseline**; 0 reached +1σ |

The losers are reported with the same detail as the winner, predictions and all.

## Requirements

Python 3.12 with `pandas`, `numpy`, `scipy`, `yfinance`, `matplotlib`, `scikit-learn`,
`pyarrow`, `PyYAML`, `pytest`, `requests`.

## Running it

```bash
python3 scripts/01_fetch.py         # network. writes data/raw/. re-runs make zero calls
python3 scripts/08_pit_universe.py  # adds historical index members -> a dated snapshot
python3 scripts/02_clean.py --as-of 2026-08-28   # raw -> validated panel + quality report
python3 scripts/03_v0.py            # the backtest -> output/*.csv
python3 scripts/04_noise.py         # 10,000 random portfolios -> the significance band
python3 scripts/07_sweep.py         # cadence x weighting grid, 8 cells (~7 min)
python3 scripts/12_signal_sweep.py  # the lookback x skip grid, 6 cells (~15 s)
python3 scripts/06_report.py        # report numbers + the four figures
python3 -m pytest tests/ -q         # 95 pass
```

Steps 1, 8 and 2 must run first, in that order. The submitted configuration is
`--calendar monthly_first_trading_day --weighting reset`; `03_v0.py` with no arguments runs
the quarterly baseline. Add `--window stress` for the Jan–Jun 2026 run.

Also available: `09_feature_diagnostics.py` (feature correlations, no PNL),
`05_v1.py --arm base|buffer|tilt|rm-solo|w3|w6|w8|no-dd|no-id` and `10_weight_sweep.py`
(the composite and its 42-cell weight surface — all rejected), and
`11_smallcap_universe.py` (the Smallcap 100 arm — rejected).

Any variant writes under `output/sweep/<cell>/` or `output/v1/<arm>/`, so a variant can
never overwrite the baseline the ledger measures against.

## Layout

```
config.yaml       every parameter. no number lives anywhere else in the repo
CLAUDE.md         the mandate, the strategy, the trial ledger (§11)
DECISIONS.md      the decision register — authoritative
WALKTHROUGH.md    the whole project explained without code
REPORT_OUTLINE.md the structure and sourcing of the 5-6 page report
docs/guidelines.docx  the organisers' Round 2 brief
src/
  config.py       load + validate; refuses to run on an unresolved decision
  calendar.py     trading calendar, rebalance dates, the t-1 formation lag
  fetch.py        network only. never imported by the analysis path
  clean.py        panel construction, corporate actions, membership mode, quality report
  membership.py   point-in-time index membership from NSE press releases
  universe.py     as-of eligibility: full window, tradeable, index member
  features.py     signal computation
  select.py       ranking, buffer, tie-break
  events.py       forced mid-cycle exits (index exits, unmodellable ex-dates)
  backtest.py     execution, costs, NAV, trades — signal- and weighting-agnostic
  metrics.py      round trips and every reported figure
  noise.py        the 10,000-draw significance band
scripts/          01_fetch 02_clean 03_v0 04_noise 05_v1 06_report 07_sweep
                  08_pit_universe 09_feature_diagnostics 10_weight_sweep
                  11_smallcap_universe 12_signal_sweep
tests/            config, causality, accounting, cleaning, selection
output/report/    generated report numbers and portfolio composition
output/figures/   the four charts
```

## Data

**Prices:** Yahoo Finance via `yfinance`, daily OHLCV, 2019-06-03 → 2026-08-24 (the extra
18 months are the lookback buffer the first rebalance needs). Close prices, never
`Adj Close`; execution at the open.

**Universe:** the NSE-published Nifty 100 and Nifty Midcap 100 constituent lists. Per the
organisers' clarification, eligibility uses **today's** lists across the whole window
(`universe.membership_mode: current_constituents`).

**Also in the repo, and this is the unusual part:** point-in-time membership rebuilt from
**27 dated NSE index-review press releases**, committed under `data/raw/press_releases/`.
Setting `membership_mode: point_in_time` re-runs the identical strategy on the universe as
it actually stood on each rebalance date. The difference between the two is **488
percentage points of index-inclusion bias**, and reporting that number is why the
reconstruction was kept — see *Known limitations*.

**Verified:** 1,786 trading days × 283 names, no interior gaps, 3 corporate-action
corrections applied and 2 handled by a forced-exit rule, 5 non-sessions excluded from the
calendar. `data/reports/data_quality.md` is generated, not hand-written.

**Not committed:** `data/raw/*.parquet` and `data/clean/*.parquet` — large, and the scripts
reproduce them. Snapshots are stamped with a fetch date and are immutable: the code refuses
to overwrite one, because Yahoo silently revises history.

## How this repo is organised to be checkable

**Every parameter is in `config.yaml`.** There is no `get(key, default)` anywhere in `src/`
— a default in a getter is a design decision made in the dark, and a test greps for the
pattern. A parameter whose decision is open is `null`, and reading it raises an error naming
the decision rather than guessing.

**`DECISIONS.md` records every choice**, including the rejected ones and the ones later
found wrong and reversed. One entry has now reversed twice, and says so.

**Variants are pre-registered before they run.** Each slate's predictions were written into
`CLAUDE.md` §11 before the first arm executed, and every arm is reported with its
predictions scored — including the ones that turned out wrong.

**Nothing is adopted for beating the benchmark alone.** 10,000 random 10-stock portfolios
run through the same engine set the bar for what luck alone produces; a change that moves
PNL by less than that band has not been shown to do anything.

**`backtest.py` is signal- and weighting-agnostic.** It takes a holdings map and nothing
about how those names were chosen, so the baseline, every variant, the benchmarks and all
10,000 noise draws run through one engine. An assertion pins the batched and scalar engines
to the rupee.

**`fetch.py` is never imported by the analysis path.** Network and analysis are separate
scripts with a file boundary between them.

**Causality is tested, not asserted.** `tests/test_causality.py` scrambles every price from
the rebalance date onward and requires the selected book to be unchanged — then scrambles
the formation window to prove the first test is not vacuous.

## Known limitations

Disclosed rather than hidden; see `DECISIONS.md` and `CLAUDE.md` §10.

- **Index-inclusion bias, mandated and measured.** Using today's constituents over 2021–25
  means a stock is eligible partly because it later joined the index. The identical rule on
  a point-in-time universe returns **+388.0%** against **+876.5%** — the bias is worth
  **488 percentage points**. The edge survives the correction (the strategy still beats
  9,996 of 10,000 random books), but the headline number is materially inflated by the
  scoring rule's own universe definition.
- **Window specificity.** 2021–25 was an exceptional period for Indian mid-caps. The
  strategy is not shown to work in general, only over the mandated window.
- **Price return only.** Dividends are excluded, understating a dividend-reinvesting book by
  roughly 9pp of median five-year return.
- **Demergers are exited, not corrected.** Positions are sold before the ex-date rather than
  adjusted by an entitlement ratio NSE does not publish. Conservative, but a rule rather
  than a correction.
- **Split-adjusted share counts.** Whole shares are floored on back-adjusted prices, so the
  trade log's share counts are not the counts a real investor would have held.
- **One excluded name.** FORCEMOT has a 41-session hole in Yahoo's history and is dropped
  from the smallcap arm's universe rather than forward-filled.
